@model IEnumerable<AquaticsStoreAssignment.Models.Accessory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AccessoryType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AccessoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AccessoryType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AccessoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AccessoryID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AccessoryID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AccessoryID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
